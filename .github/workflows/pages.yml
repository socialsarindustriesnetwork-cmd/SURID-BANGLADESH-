name: Deploy GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  validate-and-deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Reconstruct v3 release bundle
        run: |
          cat release/v3/site-v3.0.0-*.b64 | base64 --decode > /tmp/site-v3.tar.xz
          echo "05b4580b104d0940adb1f9dbc3b787edd27d20d021b87af494ce2057b71e0397  /tmp/site-v3.tar.xz" | sha256sum --check -
          rm -rf _site
          mkdir -p _site
          tar -xJf /tmp/site-v3.tar.xz -C _site

      - name: Validate site structure
        run: python3 _site/scripts/validate_site.py

      - name: Validate JavaScript
        run: |
          node --check _site/assets/js/app.js
          node --check _site/sw.js

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: _site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
